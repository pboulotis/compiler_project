program adder
    declare x, y, sum, sub, i, flag;
    {
    # main #

    x := 5;
    y := 2;
    i := 0;
    flag := 0;

    sum := x + y;
    sub := x - y;

    print(sum);
    print(sub);

    while (i < 5){
        print(i);
        i := i + 1;

        if ( i = 3){
            flag := i * 3;
            print(flag);
        };

    };

    }.